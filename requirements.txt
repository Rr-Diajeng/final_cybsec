# Core inference API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0

# Data preprocessing
pandas>=2.2.0
scikit-learn>=1.4.0
numpy>=1.26.0

# NeMo Guardrails (optional but recommended)
nemoguardrails>=0.9.0

# Fine-tuning (install separately into llamafactory-env)
# torch>=2.2.0
# transformers>=4.40.0
# bitsandbytes>=0.43.0
# peft>=0.10.0
# trl>=0.8.0
